model Technology {
  id        String             @id @default(auto()) @map("_id") @db.ObjectId
  title     String             @unique
  sectionId String?            @db.ObjectId
  featured  Boolean
  section   TechnologySection? @relation(fields: [sectionId], references: [id], onDelete: Cascade)
}

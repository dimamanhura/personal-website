model TechnologySection {
  id           String       @id @default(auto()) @map("_id") @db.ObjectId
  title        String       @unique
  logo         String
  type         String
  technologies Technology[]
}
